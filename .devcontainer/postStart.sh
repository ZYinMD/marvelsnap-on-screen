# install dependencies (node_modules) for ui:
set -e

# cd ui && bun dev # I no longer do this because it makes vscode treat the startup process as unfinished, show an everlasting spinner in the status bar, and also don't stop the "vsc-volume-bootstrap" container.
